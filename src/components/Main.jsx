const Main = () => {
  return (
    <main>
      <form className="add-ingredient-form">
        <input type="text" placeholder="masło" aria-label="Dodaj składnik" />
        <button type="submit">Dodaj składnik</button>
      </form>
    </main>
  );
};

export default Main;
